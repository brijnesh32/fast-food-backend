# React Native service modules
- api.js: axios instance (use emulator IP or dev machine IP on device)
- authService.js: register/login wrappers
- foodService.js: getFoods/createFood
- uploadService.js: uploadFile (FormData)
